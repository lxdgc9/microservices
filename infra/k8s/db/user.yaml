apiVersion: apps/v1
kind: Deployment
metadata:
  name: micro-sys-db-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-user
  template:
    metadata:
      labels:
        app: db-user
    spec:
      containers:
        - name: db-user
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: db-user
spec:
  type: NodePort
  selector:
    app: db-user
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
